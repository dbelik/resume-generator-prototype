// @NOTE: Import library functions.
// {...}

// @NOTE: Import custom functions.
// {...}

// @NOTE: Import misc.
// {...}

/**
 * TemplateNameVariants - component that's shown for TemplateName while data is loading.
 *
 * @returns JSX.Element
 */
export default function TemplateNameVariants() {
    // @NOTE: Closures.

    // @NOTE: Render component.
    return <span />;
}
